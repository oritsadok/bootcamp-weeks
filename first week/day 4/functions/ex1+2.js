

/*const calcAge = function (until, from) {
    sum = (until - from)
    return sum

}

const age = calcAge(2017, 1989)

console.log("age is " + age)*/




const calcAge = function (now, birth) {
    sum = (now - birth)
    return sum
}
const age = calcAge(2017, 1989)
let opAge = age +1
console.log("your either " + age, "or "+ opAge)



