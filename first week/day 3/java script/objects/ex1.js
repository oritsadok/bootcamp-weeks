let p1={
    name: "ori",
    age: 30,
    city: "tlv",
}

let p2={
    name: "shaked",
    age: 30,
    city: "jaffa",
}
if (p1.age==p2.age||p1.city==p2.city){
    console.log(p1.name,"want to date",p2.name)
} else{
    console.log(p1.name,"want to date",p2.name,"but couldn't")
    
}